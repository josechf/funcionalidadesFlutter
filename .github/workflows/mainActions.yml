name: primerActions

on: 
  push:
    branches: 
      - main
jobs:
  build:
    env:
      platform: aws
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v4.2.2
      - name: mensaje
        if: ${{env.platform == 'aws'}}
        run: |
          echo "usando: ${{env.platform}}"